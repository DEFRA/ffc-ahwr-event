trigger:
  branches:
    include:
      - main
  paths:
    exclude:
      - '*.md'
      - 'docs/**'

pr: none

variables:
  - name: NODE_VERSION
    value: '18.x'
  - name: BUILD_ARTIFACT_NAME
    value: 'drop'

stages:
  # ─── CI: Build, Lint, Test & Package ────────────────────────────────────────
  - stage: CI
    displayName: 'Build & Test'
    pool:
      name: DEFRA-FFC-SND2-ubuntu2204
    jobs:
      - job: BuildAndTest
        displayName: 'Install, Lint, Test & Package'
        steps:
          - task: UseNode@1
            displayName: 'Use Node.js $(NODE_VERSION)'
            inputs:
              version: $(NODE_VERSION)

          - script: npm ci
            displayName: 'Install all dependencies'

          - script: npm test
            displayName: 'Lint & unit tests'

          - task: PublishTestResults@2
            displayName: 'Publish test results'
            condition: always()
            inputs:
              testResultsFormat: JUnit
              testResultsFiles: 'test-output/junit.xml'
              failTaskOnFailedTests: true

          # Reinstall production-only deps before packaging
          - script: npm ci --omit=dev
            displayName: 'Install production dependencies only'

          - script: |
              zip -r $(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip . \
                -x '*.git*' \
                -x 'test/*' \
                -x 'test-output/*' \
                -x '*.test.js' \
                -x '.github/*' \
                -x '.gitignore' \
                -x '.pre-commit-config.yaml' \
                -x 'docker-compose.yaml' \
                -x 'jest.config.js' \
                -x 'sonar-project.properties'
            displayName: 'Package function app'
            workingDirectory: $(Build.SourcesDirectory)

          - task: PublishBuildArtifacts@1
            displayName: 'Publish deployment artifact'
            inputs:
              PathtoPublish: '$(Build.ArtifactStagingDirectory)'
              ArtifactName: $(BUILD_ARTIFACT_NAME)

  # ─── SND2 ───────────────────────────────────────────────────────────────────
  - stage: Deploy_SND2
    displayName: 'Deploy to SND2'
    dependsOn: CI
    condition: succeeded()
    variables:
      - group: ffc-ahwr-event-snd2
    pool:
      name: DEFRA-FFC-SND2-ubuntu2204
    jobs:
      - deployment: Deploy
        displayName: 'Deploy ffc-ahwr-event-snd2'
        timeoutInMinutes: 10
        environment: SND2
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureFunctionApp@2
                  displayName: 'Deploy to ffc-ahwr-event-snd2'
                  inputs:
                    connectedServiceNameARM: AZD-FFC-SND2
                    appType: functionApp
                    appName: ffc-ahwr-event-snd2
                    package: '$(Pipeline.Workspace)/$(BUILD_ARTIFACT_NAME)/*.zip'
                    deploymentMethod: auto
                    appSettings: >
                      -FUNCTIONS_WORKER_RUNTIME node
                      -WEBSITE_NODE_DEFAULT_VERSION ~18
                      -WEBSITE_RUN_FROM_PACKAGE 1
                      -ENVIRONMENT snd2
                      -AzureWebJobsStorage "$(AZURE_WEB_JOBS_STORAGE)"
                      -STORAGE_CONNECTION_STRING "$(STORAGE_CONNECTION_STRING)"
                      -APPLICATIONINSIGHTS_CONNECTION_STRING "$(APPLICATIONINSIGHTS_CONNECTION_STRING)"
                      -STORAGE_ACCOUNT_NAME "$(STORAGE_ACCOUNT_NAME)"

  # ─── DEV ────────────────────────────────────────────────────────────────────
  - stage: Deploy_DEV
    displayName: 'Deploy to DEV'
    dependsOn: Deploy_SND2
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    variables:
      - group: ffc-ahwr-event-dev
    pool:
      name: DEFRA-FFC-DEV1-ubuntu2204
    jobs:
      - deployment: Deploy
        displayName: 'Deploy ffc-ahwr-event-dev'
        timeoutInMinutes: 10
        environment: DEV
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureFunctionApp@2
                  displayName: 'Deploy to ffc-ahwr-event-dev'
                  inputs:
                    connectedServiceNameARM: AZD-FFC-DEV1
                    appType: functionApp
                    appName: ffc-ahwr-event-dev
                    package: '$(Pipeline.Workspace)/$(BUILD_ARTIFACT_NAME)/*.zip'
                    deploymentMethod: auto
                    appSettings: >
                      -FUNCTIONS_WORKER_RUNTIME node
                      -WEBSITE_NODE_DEFAULT_VERSION ~18
                      -WEBSITE_RUN_FROM_PACKAGE 1
                      -ENVIRONMENT dev
                      -AzureWebJobsStorage "$(AZURE_WEB_JOBS_STORAGE)"
                      -STORAGE_CONNECTION_STRING "$(STORAGE_CONNECTION_STRING)"
                      -APPLICATIONINSIGHTS_CONNECTION_STRING "$(APPLICATIONINSIGHTS_CONNECTION_STRING)"
                      -STORAGE_ACCOUNT_NAME "$(STORAGE_ACCOUNT_NAME)"


  # ─── TEST ───────────────────────────────────────────────────────────────────
  - stage: Deploy_TEST
    displayName: 'Deploy to TEST'
    dependsOn: Deploy_DEV
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    variables:
      - group: ffc-ahwr-event-test
    pool:
      name: DEFRA-FFC-DEV1-ubuntu2204
    jobs:
      - deployment: Deploy
        displayName: 'Deploy ffc-ahwr-event-test'
        timeoutInMinutes: 10
        environment: TEST
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureFunctionApp@2
                  displayName: 'Deploy to ffc-ahwr-event-test'
                  inputs:
                    connectedServiceNameARM: AZD-FFC-DEV1
                    appType: functionApp
                    appName: ffc-ahwr-event-test
                    package: '$(Pipeline.Workspace)/$(BUILD_ARTIFACT_NAME)/*.zip'
                    deploymentMethod: auto
                    appSettings: >
                      -FUNCTIONS_WORKER_RUNTIME node
                      -WEBSITE_NODE_DEFAULT_VERSION ~18
                      -WEBSITE_RUN_FROM_PACKAGE 1
                      -ENVIRONMENT test
                      -AzureWebJobsStorage "$(AZURE_WEB_JOBS_STORAGE)"
                      -STORAGE_CONNECTION_STRING "$(STORAGE_CONNECTION_STRING)"
                      -APPLICATIONINSIGHTS_CONNECTION_STRING "$(APPLICATIONINSIGHTS_CONNECTION_STRING)"
                      -STORAGE_ACCOUNT_NAME "$(STORAGE_ACCOUNT_NAME)"

  # ─── PRE ────────────────────────────────────────────────────────────────────
  - stage: Deploy_PRE
    displayName: 'Deploy to PRE'
    dependsOn: Deploy_TEST
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    variables:
      - group: ffc-ahwr-event-pre
    pool:
      name: DEFRA-FFC-PRE1-ubuntu2204
    jobs:
      - deployment: Deploy
        displayName: 'Deploy ffc-ahwr-event-pre'
        timeoutInMinutes: 10
        environment: PRE
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureFunctionApp@2
                  displayName: 'Deploy to ffc-ahwr-event-pre'
                  inputs:
                    connectedServiceNameARM: AZP-FFC-PRE1
                    appType: functionApp
                    appName: ffc-ahwr-event-pre
                    package: '$(Pipeline.Workspace)/$(BUILD_ARTIFACT_NAME)/*.zip'
                    deploymentMethod: auto
                    appSettings: >
                      -FUNCTIONS_WORKER_RUNTIME node
                      -WEBSITE_NODE_DEFAULT_VERSION ~18
                      -WEBSITE_RUN_FROM_PACKAGE 1
                      -ENVIRONMENT pre
                      -AzureWebJobsStorage "$(AZURE_WEB_JOBS_STORAGE)"
                      -STORAGE_CONNECTION_STRING "$(STORAGE_CONNECTION_STRING)"
                      -APPLICATIONINSIGHTS_CONNECTION_STRING "$(APPLICATIONINSIGHTS_CONNECTION_STRING)"
                      -STORAGE_ACCOUNT_NAME "$(STORAGE_ACCOUNT_NAME)"

  # ─── PRD ────────────────────────────────────────────────────────────────────
  - stage: Deploy_PRD
    displayName: 'Deploy to PRD'
    dependsOn: Deploy_PRE
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    variables:
      - group: ffc-ahwr-event-prd
    pool:
      name: DEFRA-FFC-PRD1-ubuntu2204
    jobs:
      - deployment: Deploy
        displayName: 'Deploy ffc-ahwr-event-prd'
        timeoutInMinutes: 10
        environment: PRD
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureFunctionApp@2
                  displayName: 'Deploy to ffc-ahwr-event-prd'
                  inputs:
                    connectedServiceNameARM: AZR-FFC-PRD1
                    appType: functionApp
                    appName: ffc-ahwr-event-prd
                    package: '$(Pipeline.Workspace)/$(BUILD_ARTIFACT_NAME)/*.zip'
                    deploymentMethod: auto
                    appSettings: >
                      -FUNCTIONS_WORKER_RUNTIME node
                      -WEBSITE_NODE_DEFAULT_VERSION ~18
                      -WEBSITE_RUN_FROM_PACKAGE 1
                      -ENVIRONMENT prd
                      -AzureWebJobsStorage "$(AZURE_WEB_JOBS_STORAGE)"
                      -STORAGE_CONNECTION_STRING "$(STORAGE_CONNECTION_STRING)"
                      -APPLICATIONINSIGHTS_CONNECTION_STRING "$(APPLICATIONINSIGHTS_CONNECTION_STRING)"
                      -STORAGE_ACCOUNT_NAME "$(STORAGE_ACCOUNT_NAME)"
